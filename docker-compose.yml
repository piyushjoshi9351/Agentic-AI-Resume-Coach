version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AI_PROVIDER=local
      - PYTHONUNBUFFERED=1
    volumes:
      - ./backend:/app/backend:delegated
    restart: unless-stopped
